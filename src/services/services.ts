/* eslint-disable import/no-dynamic-require */
/* eslint-disable @typescript-eslint/explicit-function-return-type */
/* eslint-disable @typescript-eslint/no-this-alias */
/*
const services = () => {
  const allServices:any = this;
  const serviceList = [
    {
      name: 'ls',
      source: './services/lsService.ts',
    },
    {
      name: 'data',
      source: './services/dataService.ts',
    },
    {
      name: 'repo',
      source: './services/repoService.ts',
    },
    {
      name: 'user',
      source: './services/userService.ts',
    },
  ];

  serviceList.forEach((service) => {
    // creating instances of modules inside of repos
    // eslint-disable-next-line global-require
    allServices[service.name] = require(service.source);
  });

  return allServices;
};

export default services();
*/
export {};
